# User flows — Kartka

Наскрізні потоки для **ядра MVP** (jobs із [`research/jtbd.md`](research/jtbd.md)).
Вузли `[у дужках]` — екрани зі [`sitemap.md`](sitemap.md) («Екрани»); ромби
`{питання?}` — точки рішень; стани (холодний старт / офлайн / error) — окремими
вузлами. **Межа:** жоден вузол не інтерпретує дані (норм/зон/оцінок немає — AVOID).

---

## R1 — ДОДАТИ документ  *(ризик №1, ціль ≤3 тапи · primary: Координатор)*

```mermaid
flowchart TD
  start(["Відкрив застосунок"]) --> auth{"Увійшов?"}
  auth -->|ні| login["Екран входу — email+пароль"]
  login --> cold
  auth -->|так| cold{"Перший запуск / порожньо?"}

  cold -->|"так (новий)"| onb["Онбординг: Додай СЕБЕ → self-профіль"]
  onb --> empty["СТАН: порожній дашборд (холодний старт)"]
  empty --> cta{"Що далі?"}
  cta -->|"CTA: додай перший запис"| sheet
  cta -->|"не зрозумів навіщо"| churn["ГЛУХИЙ КУТ: пішов, нічого не додав (ризик adoption)"]

  cold -->|ні| dash["Дашборд профілю"]
  dash --> who{"Профіль уже активний?"}
  who -->|ні| switch["Перемикач профілю"]
  switch --> dash
  who -->|так| plus["Тап ➕ (1)"]
  plus --> sheet["Швидке додавання — вибір типу"]
  sheet --> type{"Який тип? (2)"}
  type -->|ліки / аналіз / візит| other["інші потоки R3/R4"]
  type -->|документ| adddoc["Додати документ — фото-перший"]

  adddoc --> src{"Фото чи з галереї?"}
  src -->|камера| cam{"Дозвіл на камеру?"}
  cam -->|ні| gal["З галереї / файл"]
  cam -->|так| shot["Знімок (3)"]
  src -->|галерея| gal
  shot --> savecheck
  gal --> savecheck{"Збереглося?"}
  savecheck -->|ні| retry["Помилка — Повторити"]
  retry --> savecheck
  retry -->|вийшов| deadlost["ГЛУХИЙ КУТ: знімок втрачено"]
  savecheck -->|так| net{"Є мережа?"}
  net -->|ні| queue["Збережено локально, синк пізніше"]
  net -->|так| ok["Збережено ✓ — дата=сьогодні, профіль=активний"]
  queue --> ok
  ok --> view["Документ — перегляд (тип/лікар можна дозаповнити)"]
```

**Рішення:** `Увійшов?` · `Перший запуск/порожньо?` (холодний старт) · `Профіль
уже активний?` (інакше — перемикач) · `Який тип?` (глобальний «+») · `Фото чи з
галереї?` · `Дозвіл на камеру?` · `Збереглося?` · `Є мережа?`.
**Стани:** холодний старт (порожній дашборд + CTA), локальне збереження офлайн
(черга синку), помилка збереження (повтор).
**Щасливий шлях ≤3 тапи:** `➕ (1) → Документ (2) → Знімок (3)` → збережено
(дата/профіль авто, фото-перший комітить одразу).
**Тупики:** новий користувач не зрозумів навіщо → пішов (adoption-ризик);
вийшов під час помилки → знімок втрачено.

---

## R2 — ПОКАЗАТИ анамнез лікарю  *(момент цінності · primary)*

```mermaid
flowchart TD
  start["Дашборд профілю"] --> tap["Тап 📤 Показати (1)"]
  tap --> who{"Профіль уже обрано?"}
  who -->|ні| switch["Перемикач профілю"]
  switch --> tap
  who -->|так| screen["Анамнез-PDF — генерація+перегляд"]

  screen --> scope{"Повний анамнез чи лише критичне?"}
  scope -->|повний| sections["Вибір розділів + мова (укр/англ)"]
  scope -->|критичне| sections
  sections --> net{"Є мережа?"}

  net -->|так| gen["Генерація PDF на сервері — loading (2)"]
  gen --> genok{"Згенеровано?"}
  genok -->|ні| generr["Помилка — Повторити / показати кешований"]
  generr --> gen
  genok -->|так| sparse

  net -->|ні| cache{"Є офлайн-копія анамнезу?"}
  cache -->|так| sparse{"Достатньо даних?"}
  cache -->|ні| deadoff["ГЛУХИЙ КУТ: офлайн і нема кешу — потрібна мережа"]

  sparse -->|"мало (новий профіль)"| weak["Неповна картина — позначка «це не вся історія»"]
  weak --> show
  sparse -->|так| show["Показати лікарю на екрані ✓ (2 тапи)"]
  show --> shareq{"Віддати копію?"}
  shareq -->|ні| done["Готово — показано"]
  shareq -->|так| how{"Як поділитися? (3)"}
  how -->|файл| shared["Поділено ✓"]
  how -->|лінк| shared
  how -->|друк| shared
```

**Рішення:** `Профіль уже обрано?` · `Повний чи лише критичне?` · `Є мережа?`
(офлайн → кеш) · `Згенеровано?` · `Достатньо даних?` · `Як поділитися?`.
**Стани:** loading (серверна генерація), помилка генерації (повтор/кеш), офлайн
(кешований анамнез — jtbd сц.2), неповна картина (чесна позначка, **не** оцінка).
**Щасливий шлях:** `📤 Показати (1) → Згенерувати (2)` → показ на екрані = **2
тапи**; `+ Поділитися (3)` = віддати файл.
**Тупики:** офлайн **і** немає офлайн-копії (ще жодного разу не генерували) →
потрібна мережа.

---

## R5 — ЕКСТРЕНА картка  *(гарячий момент · офлайн обов'язково · використовує Носій/інший)*

```mermaid
flowchart TD
  trigger(["Гарячий момент — потрібні критичні дані"]) --> entry{"Як відкривають?"}
  entry -->|"офлайн-ярлик (home screen)"| offcache["Офлайн-кеш екстреної картки — без логіну"]
  entry -->|у застосунку| header["Шапка → 🆘 Екстрене (1)"]

  header --> net{"Є мережа?"}
  net -->|ні| offcache
  net -->|так| online["Екстрена картка — перегляд"]

  offcache --> card
  online --> card{"Картку заповнено?"}
  card -->|"ні (порожня)"| deadempty["ГЛУХИЙ КУТ: критичних даних нема — холодний старт б'є саме тут"]
  card -->|так| show["Критичне: алергії · хроніка · група крові · ліки · контакти ✓"]

  show --> hosp{"Потрібна лікарня поряд?"}
  hosp -->|так| maps["Зовнішні мапи — handoff, без даних пацієнта"]
  hosp -->|ні| done["Готово — критичне видно за секунди"]
```

**Рішення:** `Як відкривають?` (офлайн-ярлик без логіну vs у застосунку) · `Є
мережа?` (офлайн → кеш) · `Картку заповнено?` · `Потрібна лікарня поряд?`.
**Стани:** **офлайн-кеш** (service worker, без повного входу — CLAUDE §6) —
обов'язкова гілка; порожня картка (холодний старт). Кеш містить картки всіх
членів, тож вибір члена працює офлайн.
**Щасливий шлях:** `🆘 / офлайн-ярлик (1)` → критичне видно за **1 тап**, офлайн.
**Тупики:** базову картку не заповнено → у кризі нема що показати (холодний старт
вражає саме екстрений сценарій — найдорожчий тупик).
**Handoff:** «Лікарня/травмпункт поряд» → зовнішні мапи ОС, без передачі даних
пацієнта (не екран, не каталог).

---

> **Межа дотримана:** жоден вузол не інтерпретує дані — ніяких норм-зон, оцінок чи
> «відхилень» (AVOID, SaMD). Анамнез і екстрена картка лише **показують** факти;
> «неповна картина» — це чесна позначка повноти, не оцінка стану.
