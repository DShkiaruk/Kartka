# Sitemap — Kartka

Інформаційна архітектура, **крок 1: сутності** (об'єкти й зв'язки, ще НЕ екрани й
не навігація). Виведено з [`research/jtbd.md`](research/jtbd.md) (jobs MAIN/R1–R5
+ 10 сценаріїв) і [`research/personas.md`](research/personas.md); узгоджено з
обраним коренем IA — **профіль-перший каркас + дашборд** ([`research/patterns-information-architecture.md`](research/patterns-information-architecture.md)),
тож усе нанизується на **Профіль**. Межі — з [`CLAUDE.md`](CLAUDE.md).

Теги: `[?]` — припущення / під питанням; **AVOID** — свідомо НЕ робимо.

---

## Сутності

### Структурні (кореневі)

#### 1. Акаунт / Домогосподарство
- **Поля:** email + пароль (Auth); налаштування; **містить профілі**.
- **Job:** MAIN (тримає всю картину родини в одному місці); сц.10 (кілька профілів під одним входом).
- **Зв'язки:** Акаунт **1 — N** Профіль. Верхній контейнер усього.
- *Межа:* один акаунт на домогосподарство; multi-user/запрошення — поза MVP (CLAUDE §6).

#### 2. Профіль члена родини  ← **структурна вісь IA**
- **Поля:** ім'я, аватар/колір (для впізнавання, сц.10), дата народження, стать `[?]`, стосунок (я / дитина / батько…).
- **Частина «Базова картка»:** алергії [], хроніка [], група крові, екстрені контакти [] (фіча 1).
- **Job:** MAIN (картина **на члена**); R5 (екстрене — на конкретного члена, сц.6); наскрізно сц.10 (не сплутати, чий запис). Базова картка живить R5 і R2.
- **Зв'язки:** Профіль **1 — N** Документ / Ліки / Показник / Візит; Профіль **1 — 1** Базова картка. Батьківський об'єкт для всього контенту.

### Контентні (живуть під профілем)

#### 3. Документ
- **Поля:** файл/фото (вкладення), дата (автодефолт «сьогодні»), **тип** (пресет), лікар/заклад — *текст, опційно*, нотатка `[?]`.
- **Job:** **R1** — зафіксувати вхідне, поки не загубив (сц.4). Головний ризик-вводу.
- **Зв'язки:** Документ **N — 1** Профіль. Опційно: документ-результат ↔ Вимірювання показника; документ-рецепт ↔ Ліки `[?]` (зв'язок-вкладення, не обов'язковий).
- *Межа:* **без AI-розбору**; тип — контрольований словник, не сутність (див. нижче).

#### 4. Ліки (препарат члена)
- **Поля:** назва, дозування, схема/частота, **період** (курс із «days limit» vs постійно), статус (активний / завершений), відмітка «скоро закінчується» (сц.9).
- **Частина «Журнал прийому»:** події {дата-час, прийнято/пропущено} з миттєвим undo (сц.1).
- **Job:** **R3** — ліки в часі й між людьми: що прийнято сьогодні (сц.1), точний список комусь (сц.8), не лишитись без (сц.9).
- **Зв'язки:** Ліки **N — 1** Профіль; Ліки **1 — N** Прийом (журнал). Опційно — нагадування про поповнення (див. Нагадування).

#### 5. Показник аналізу (серія значень у часі)
- **Поля:** назва показника (з довідника або свій), одиниця.
- **Частина «Вимірювання»:** записи {дата, значення, (+ опц. файл-джерело)} — фіча 4.
- **Job:** **R4** — бачити **динаміку** показника, не окремі цифри (сц.7).
- **Зв'язки:** Показник **N — 1** Профіль; Показник **1 — N** Вимірювання; Вимірювання опційно **↔** Документ (джерело).
- ⚠️ **AVOID (межа SaMD, CLAUDE §4):** **лише сира крива** значень. **НЕ** зберігаємо й не показуємо референсні норми, зони «в межах/поза», кольори-оцінки, «відхилення» чи обчислені висновки — це інтерпретація = медвиріб. Окремої сутності «норма/зона» **немає**.

#### 6. Візит (з нагадуванням)
- **Поля:** тип/назва (чекап, стоматолог, спеціаліст), дата/час, **повторюваність** (рік / 6 міс / кастом), випередження нагадування (тиждень/день), тихі години, статус (попереду / відбувся).
- **Job:** **R4** — не пропустити планове (сц.5).
- **Зв'язки:** Візит **N — 1** Профіль.

### Похідні сутності (проєкції — не нові дані, а композиція наявного)

#### 7. Анамнез-експорт (PDF)
- **Що це:** **проєкція** даних профілю в самодостатній документ; нових даних не зберігає.
- **Конфіг:** вибір розділів, обсяг (повний / лише критичне), мова (укр/англ), офлайн-доступ.
- **Складається з:** Базова картка + поточні Ліки + останні Показники + Візити + (опц.) Документи.
- **Job:** **R2** — показати повну картину іншому за секунди (сц.2, 3, 8).
- **Зв'язки:** проєкція **Профілю** (компонує №2–6).

#### 8. Екстрена картка
- **Що це:** **проєкція** критичного підмножини; доступна офлайн і без повного входу (сц.6).
- **Складається з:** Базова картка (алергії / хроніка / група крові) + активні Ліки + екстрені контакти.
- **Job:** **R5** — готовність до критичного моменту, навіть без мене (сц.6).
- **Зв'язки:** проєкція **Профілю** (база картки + №4 активні + контакти).

---

## Під питанням / навмисно НЕ сутності

- **Нагадування — окрема сутність чи властивість?** `[?]` Схиляюсь: **властивість/подія** Візиту (фіча 5, сц.5) та опційно Ліки-курсу (поповнення, сц.9), а не самостійний верхній об'єкт — бо завжди прикріплене до чогось. Винести в окрему сутність лише якщо з'являться вільностоячі нагадування.
- **Лікар / Заклад — поле, не сутність.** Зберігаємо як **текст** на Документі/Візиті. **Каталог закладів НЕ будуємо** (CLAUDE фіча 7). Жодної сутності-довідника лікарів.
- **Тип документа — контрольований словник (enum), не сутність.** Пресети: виписка/епікриз, направлення, рецепт, результат аналізу, довідка, висновок (CLAUDE фіча 2).
- **Довідник показників — службовий reference, не користувацька сутність** `[?]`. Каталог популярних показників (гемоглобін, глюкоза…) з одиницями — seed/автодоповнення для №5; інфраструктура, не дані користувача.
- **Співкоординатор / запрошення / multi-user — поза MVP** (CLAUDE §6). З'являється в сценаріях (сц.1, 8), але в MVP закрито шером PDF/списку, не спільним об'єктом-доступом. Сутності зараз не заводимо.
- **«Лікарня/травмпункт поряд» — дія-handoff, не дані.** Зовнішній deep-link у мапи ОС, без власних об'єктів і без передачі даних пацієнта (поза основним MVP, CLAUDE фіча 7).
- ❌ **Норма / зона / відхилення / оцінка — AVOID.** Свідомо НЕ сутності: інтерпретація = SaMD (CLAUDE §4). Показник тримає лише сирі значення.

---

## Карта зв'язків (стисло)

```
Акаунт / Домогосподарство
  └─ 1—N Профіль  ← вісь IA
        ├─ 1—1 Базова картка (алергії, хроніка, група крові, контакти)
        ├─ 1—N Документ            (R1 · сц.4)        [тип=enum; лікар=текст]
        ├─ 1—N Ліки                (R3 · сц.1,8,9)    └─ 1—N Прийом (журнал)
        ├─ 1—N Показник            (R4 · сц.7)        └─ 1—N Вимірювання ↔ Документ(опц.)
        └─ 1—N Візит+нагадування   (R4 · сц.5)

  Проєкції профілю (без нових даних):
   • Анамнез-PDF      (R2 · сц.2,3,8) = базова картка + ліки + показники + візити + документи
   • Екстрена картка  (R5 · сц.6)     = базова картка + активні ліки + контакти
```

> Кожна сутність має ≥1 job. Сутностей під фічі поза ядром MVP не заводив.
> Наступний крок IA (окремо, не тут): як ці об'єкти лягають на екрани/навігацію.
